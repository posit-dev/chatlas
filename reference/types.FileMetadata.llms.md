# types.FileMetadata

``` python
types.FileMetadata()
```

Normalized metadata for a provider-hosted file.

## Attributes

| Name | Description |
|----|----|
| [extra](#chatlas.types.FileMetadata.extra) | The provider’s own file object, unmodified, for fields this class doesn’t |
