name: Test

on:
  workflow_dispatch:
  push:
    branches: ["main", "rc-*"]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  release:
    types: [published]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install the project
        run: uv sync --python ${{ matrix.python-version }} --all-extras

      - name: Check tests (VCR replay)
        run: |
          uv run pytest tests/ -v \
            --ignore=tests/test_batch_chat.py \
            --ignore=tests/test_inspect.py \
            --ignore=tests/test_mcp_client.py \
            --ignore=tests/test_mcp_servers.py \
            --ignore=tests/test_tokens.py
        env:
          # Skip providers that don't work with VCR or don't have cassettes
          # Bedrock uses AWS SSO which requires live credential fetching - incompatible with VCR
          TEST_BEDROCK: "false"
          TEST_SNOWFLAKE: "false"
          # Portkey API was failing during cassette recording
          PORTKEY_API_KEY: ""

      - name: Check types
        if: ${{ matrix.python-version != '3.9' }}
        run: make check-types

      - name: Check formatting
        run: make check-format
